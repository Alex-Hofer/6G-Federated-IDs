---
phase: 08-security-integrity
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - tests/test_security.py
  - tests/test_preprocess.py
autonomous: true
requirements: [SEC-09]

must_haves:
  truths:
    - "Tests verify DataValidationError is raised for invalid data in loader, preprocess, partition"
    - "Tests verify fedavg_aggregate rejects empty results"
    - "Tests verify fedavg_aggregate skips zero-example clients and errors when all are zero"
    - "Tests verify fedavg_aggregate skips NaN-parameter clients and errors when all have NaN"
    - "Tests verify scaler JSON save/load round-trip produces correct StandardScaler"
    - "Tests verify scaler JSON load rejects mismatched array lengths and non-finite values"
    - "Tests verify invalid log_level raises ValueError"
    - "Tests verify invalid config_path extension raises ValueError"
    - "Tests verify config dict is not mutated during FL training"
    - "Existing test_preprocess.py artifact check updated for scaler.json"
  artifacts:
    - path: "tests/test_security.py"
      provides: "Comprehensive validation gate and security tests"
      min_lines: 150
    - path: "tests/test_preprocess.py"
      provides: "Updated artifact check for scaler.json"
      contains: "scaler.json"
  key_links:
    - from: "tests/test_security.py"
      to: "src/federated_ids/exceptions.py"
      via: "import and assert raises DataValidationError"
      pattern: "DataValidationError"
    - from: "tests/test_security.py"
      to: "src/federated_ids/fl/server.py"
      via: "import and test fedavg_aggregate"
      pattern: "fedavg_aggregate"
    - from: "tests/test_security.py"
      to: "src/federated_ids/config.py"
      via: "import and test validate_log_level, validate_config_path"
      pattern: "validate_log_level"
---

<objective>
Create comprehensive tests for all validation gates added in Plans 01 and 02, and update existing test references.

Purpose: SEC-09 requires tests that prove validation gates catch invalid inputs (the "assert bypass scenario") and that fedavg edge cases produce correct errors. Without tests, the validation changes are unverifiable and could regress.

Output: New test_security.py with validation gate tests, updated test_preprocess.py artifact check.
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-security-integrity/08-CONTEXT.md
@.planning/phases/08-security-integrity/08-RESEARCH.md
@.planning/phases/08-security-integrity/08-01-SUMMARY.md
@.planning/phases/08-security-integrity/08-02-SUMMARY.md

@tests/conftest.py
@tests/test_preprocess.py

<interfaces>
<!-- Key types and contracts from Plans 01 and 02. -->

From src/federated_ids/exceptions.py (created in Plan 01):
```python
class DataValidationError(ValueError):
    """Raised when input data fails validation checks."""
    pass
```

From src/federated_ids/fl/server.py (modified in Plan 02):
```python
def fedavg_aggregate(
    results: list[tuple[list[np.ndarray], int]],
) -> list[np.ndarray]:
    # Raises DataValidationError on empty results or all-invalid clients
    # Skips zero-example clients with warning
    # Skips NaN-parameter clients with warning
```

From src/federated_ids/data/preprocess.py (modified in Plan 01):
```python
def _save_scaler_json(scaler: StandardScaler, path: str) -> None:
    # Saves versioned JSON envelope

def _load_scaler_json(path: str) -> StandardScaler:
    # Validates array lengths and finite values
    # Raises DataValidationError on validation failure
```

From src/federated_ids/config.py (modified in Plan 02):
```python
def validate_log_level(level: str) -> str:
    # Raises ValueError if level not in allowed set

def validate_config_path(path: str) -> str:
    # Raises ValueError if not .yaml/.yml extension
```

From tests/test_preprocess.py line 204:
```python
# Currently checks for "scaler.pkl" -- needs update to "scaler.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_security.py with validation gate tests</name>
  <files>tests/test_security.py</files>
  <behavior>
    - TestAssertReplacements:
      - test_loader_inf_raises: DataFrame with Inf values -> DataValidationError with "Inf" and count in message
      - test_loader_nan_raises: DataFrame with NaN values -> DataValidationError with "NaN" and count in message
      - test_loader_bad_labels_raises: DataFrame with labels {0, 1, 2} -> DataValidationError with actual labels in message
      - test_preprocess_nan_raises: X_train with NaN after scaling -> DataValidationError
      - test_preprocess_inf_raises: X_train with Inf after scaling -> DataValidationError
      - test_preprocess_dtype_raises: X_train with float64 dtype -> DataValidationError with actual dtype
      - test_partition_ratio_raises: Partition with >5% class ratio deviation -> DataValidationError
    - TestFedAvgValidation:
      - test_empty_results: empty list -> DataValidationError with "empty results" in message
      - test_zero_example_clients_skipped: mix of valid and zero-example clients -> aggregation succeeds using only valid clients
      - test_all_zero_example_raises: all clients zero examples -> DataValidationError with "All clients filtered"
      - test_nan_params_skipped: mix of valid and NaN-param clients -> aggregation succeeds using only valid clients
      - test_all_nan_raises: all clients have NaN params -> DataValidationError
      - test_valid_aggregation: normal inputs -> correct weighted average (regression test)
    - TestScalerJson:
      - test_save_load_roundtrip: save StandardScaler to JSON, load back, verify mean_/scale_/var_ match original
      - test_json_envelope_format: saved JSON has version, type, params keys
      - test_load_mismatched_lengths: JSON with array length != n_features_in_ -> DataValidationError
      - test_load_non_finite_values: JSON with NaN/Inf in arrays -> DataValidationError
      - test_pkl_migration_error: only .pkl exists -> DataValidationError with "regenerate" instruction
    - TestConfigValidation:
      - test_valid_log_levels: all 5 valid levels accepted
      - test_invalid_log_level: "VERBOSE", "info" (lowercase), "" -> ValueError
      - test_valid_config_paths: "config.yaml", "path/to/config.yml" accepted
      - test_invalid_config_extension: "config.json", "config.txt" -> ValueError
    - TestConfigSafety:
      - test_no_config_mutation: pass config dict through FL training path, verify "_device" key NOT added
  </behavior>
  <action>
Create `tests/test_security.py` following the existing test conventions in the project (class-based tests, pytest fixtures, descriptive method names).

Use `pytest.raises(DataValidationError)` for data validation checks and `pytest.raises(ValueError)` for config validation checks.

For TestAssertReplacements: These tests need to exercise the actual validation code paths in loader.py, preprocess.py, partition.py. Use minimal synthetic data that triggers each validation gate. Import the validation-containing functions directly and test them with crafted invalid inputs. If direct testing is difficult due to complex setup, test the validation logic in isolation by extracting the check patterns.

For TestFedAvgValidation: Create synthetic numpy arrays as client parameters. Test fedavg_aggregate directly with crafted inputs:
- Empty list: `fedavg_aggregate([])`
- Zero examples: `fedavg_aggregate([([np.array([1.0])], 0)])`
- NaN params: `fedavg_aggregate([([np.array([float('nan')])], 10)])`
- Mixed valid/invalid: ensure the valid clients still aggregate correctly

For TestScalerJson: Import `_save_scaler_json` and `_load_scaler_json` from preprocess.py. Use `tmp_path` fixture for file operations. Fit a StandardScaler on small synthetic data, save/load, compare attributes.

For TestConfigValidation: Import `validate_log_level` and `validate_config_path` from config.py. Parametrize where appropriate.

For TestConfigSafety: Create a config dict, verify `"_device"` key is never injected. This can test `save_fl_metrics` directly -- pass a config dict and verify it's unchanged after the call.

Follow project convention: Google-style docstrings on test classes, descriptive test method names.
  </action>
  <verify>
    <automated>cd C:/Users/Carlo/Desktop/Familie/Alex/Anderes/Projects/6g-federated-ids && python -m pytest tests/test_security.py -x -v</automated>
  </verify>
  <done>
    - tests/test_security.py exists with 20+ test methods across 5 test classes
    - All tests pass
    - Tests cover: assert replacements (7 tests), fedavg validation (6 tests), scaler JSON (5 tests), config validation (4 tests), config safety (1 test)
    - Tests use DataValidationError and ValueError assertions correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_preprocess.py artifact check and run full suite</name>
  <files>tests/test_preprocess.py</files>
  <action>
1. In `tests/test_preprocess.py`:
   - Find the artifact check that references `scaler.pkl` (approximately line 204, in TestArtifactsSaved class)
   - Change `scaler.pkl` to `scaler.json`
   - If the test verifies file content, update to check for JSON structure (version, type, params keys) instead of pickle binary

2. Run the full test suite to confirm no regressions from ALL Phase 8 changes:
   - `python -m pytest tests/ -x` must pass completely

Note: This task is intentionally small and combined with the full-suite validation as a final gate.
  </action>
  <verify>
    <automated>cd C:/Users/Carlo/Desktop/Familie/Alex/Anderes/Projects/6g-federated-ids && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - test_preprocess.py checks for scaler.json instead of scaler.pkl
    - Full test suite passes (all existing tests + new test_security.py)
    - Zero regressions from Phase 8 changes
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_security.py -x -v` -- all security tests pass
- `python -m pytest tests/ -x` -- full suite passes
- `python -m pytest tests/test_security.py --co -q | wc -l` shows 20+ test cases collected
</verification>

<success_criteria>
- tests/test_security.py exists with comprehensive coverage of all SEC-01 through SEC-08 validation behaviors
- test_preprocess.py updated for scaler.json artifact
- Full test suite passes with zero failures
- Each validation gate (assert replacements, fedavg, scaler JSON, config) has dedicated test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-integrity/08-03-SUMMARY.md`
</output>
