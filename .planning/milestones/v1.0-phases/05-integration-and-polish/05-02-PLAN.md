---
phase: 05-integration-and-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - README.md
  - docs/confusion_matrix.png
  - docs/convergence.png
autonomous: false
requirements:
  - INFR-02

must_haves:
  truths:
    - "README contains complete setup instructions from clone to first run"
    - "README documents the federated-ids-run-all command as primary entry point"
    - "README includes troubleshooting section covering common issues"
    - "README embeds output screenshots from docs/ folder"
    - "Someone unfamiliar with the project can reproduce the experiment by following only the README"
  artifacts:
    - path: "README.md"
      provides: "Thesis-reproducibility guide with setup, usage, configuration, troubleshooting"
      min_lines: 150
    - path: "docs/confusion_matrix.png"
      provides: "Screenshot of confusion matrix output for README embedding"
    - path: "docs/convergence.png"
      provides: "Screenshot of convergence plot output for README embedding"
  key_links:
    - from: "README.md"
      to: "docs/confusion_matrix.png"
      via: "Markdown image embed"
      pattern: "docs/confusion_matrix\\.png"
    - from: "README.md"
      to: "docs/convergence.png"
      via: "Markdown image embed"
      pattern: "docs/convergence\\.png"
---

<objective>
Rewrite the README as a thesis-reproducibility guide and add output screenshots so someone reading the thesis can clone and reproduce the exact experiment.

Purpose: Documentation completes the project -- a reader of the thesis can follow the README from clone to reproduction with zero ambiguity. This fulfills INFR-02 (reproducibility via documented hyperparameters).
Output: Rewritten `README.md`, output screenshots in `docs/`
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-polish/05-CONTEXT.md
@.planning/phases/05-integration-and-polish/05-RESEARCH.md
@.planning/phases/05-integration-and-polish/05-01-SUMMARY.md

@README.md
@pyproject.toml
@config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README as thesis-reproducibility guide</name>
  <files>README.md, docs/confusion_matrix.png, docs/convergence.png</files>
  <action>
Rewrite `README.md` entirely as a thesis-reproducibility guide in English. The target reader is someone who has read the thesis and wants to reproduce the exact experiment.

**Structure the README with these sections (ordering at Claude's discretion but all must be present):**

1. **Title + Project Summary**: "6G Federated IDS" -- 2-3 sentence description of what the project does (privacy-preserving DDoS detection via Federated Learning on CICIDS2017, no raw data sharing)

2. **Architecture Overview**: Brief description of the pipeline stages (data preprocessing -> federated training with FedAvg -> evaluation). Mention the MLP model, Flower framework, and that clients train locally on IID partitions.

3. **Prerequisites**: Python 3.11+, ~2GB disk space for dataset + outputs, GPU optional (auto-detected)

4. **Setup / Installation**: Step-by-step from `git clone` to `pip install -e ".[dev]"`. Include venv creation for Linux/macOS and Windows (both cmd and Git Bash). Show exact commands.

5. **Data Download**: CICIDS2017 dataset -- link to UNB page, which file to download (`Friday-WorkingHours-Afternoon-DDos.pcap_ISCX.csv`), where to place it (`data/raw/`)

6. **Quick Start**: The `federated-ids-run-all` command as the PRIMARY way to run. Show the single command and explain what it does (chains all three stages). Mention `--config` flag.

7. **Individual Stage Commands**: Document all 5 console scripts:
   - `federated-ids-preprocess`
   - `federated-ids-train`
   - `federated-ids-train-fl`
   - `federated-ids-evaluate`
   - `federated-ids-run-all`
   Brief description of each with example usage.

8. **Configuration**: Document `config/default.yaml` structure -- data, model, training, federation sections. List key hyperparameters with their defaults and what they control. Emphasize that all hyperparameters are centralized here for reproducibility.

9. **Output Structure**: Document the `outputs/` directory tree:
   ```
   outputs/
     checkpoints/global_model.pt
     metrics/fl_metrics.json
     plots/confusion_matrix.png
     plots/convergence.png
     plots/client_comparison.png
     plots/classification_report.txt
     tensorboard/
   ```

10. **Results**: Embed the screenshots from `docs/` folder using relative markdown image syntax: `![Confusion Matrix](docs/confusion_matrix.png)` and `![Convergence](docs/convergence.png)`. Add placeholder text noting these are from a run with default configuration.

11. **TensorBoard**: How to view real-time training metrics: `tensorboard --logdir outputs/tensorboard`

12. **Running Tests**: `python -m pytest tests/ -v` for full suite, `python -m pytest tests/test_integration.py -v` for integration test only

13. **Troubleshooting** (per user decision, must include these specific items):
    - Missing CSVs: error message when data/raw/ is empty, where to download
    - CUDA OOM: reduce batch_size or hidden_layers in config, or set device to cpu
    - TensorBoard port conflicts: `--port` flag to use different port
    - Python version: requires 3.11+, check with `python --version`

14. **Project Structure**: Brief tree showing `src/federated_ids/` modules and their purpose

15. **License** (if applicable) or omit if no license file exists

**Screenshots for docs/ folder:**
- Create `docs/` directory if it doesn't exist
- The user needs to generate actual screenshots by running the pipeline with real data first
- Create placeholder PNG files (or copy from `outputs/plots/` if they exist from a prior run)
- If `outputs/plots/confusion_matrix.png` and `outputs/plots/convergence.png` exist, copy them to `docs/`
- If they don't exist, add a note in the README: "Run the pipeline once with real data, then copy the plots: `cp outputs/plots/confusion_matrix.png docs/` and `cp outputs/plots/convergence.png docs/`"

**Constraints:**
- English language (per user decision)
- Do NOT modify any source code files
- README should be 150-250 lines
- Use fenced code blocks for all commands
- All paths assume commands are run from project root directory
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "
import pathlib
readme = pathlib.Path('README.md')
assert readme.exists(), 'README.md missing'
content = readme.read_text()
assert len(content.splitlines()) >= 150, f'README too short: {len(content.splitlines())} lines'
for section in ['Setup', 'federated-ids-run-all', 'Troubleshooting', 'Configuration', 'Data Download']:
    assert section in content, f'Missing section: {section}'
assert 'docs/confusion_matrix.png' in content, 'Missing confusion matrix image embed'
assert 'docs/convergence.png' in content, 'Missing convergence image embed'
print('README validation passed')
"</automated>
  </verify>
  <done>
    - README.md is rewritten as a comprehensive thesis-reproducibility guide (150+ lines)
    - Contains all required sections: setup, data download, quick start, configuration, output structure, troubleshooting
    - Documents `federated-ids-run-all` as the primary entry point
    - Embeds screenshots from docs/ folder
    - Troubleshooting covers: missing CSVs, CUDA OOM, TensorBoard port conflicts, Python version
    - docs/ directory exists with screenshot files (real or placeholder instructions)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README quality and completeness</name>
  <files>README.md</files>
  <action>
Human reviews the README for quality, completeness, and accuracy as a thesis-reproducibility guide.

What was built: Complete README rewrite as thesis-reproducibility guide with embedded output screenshots. The README covers the full journey from clone to reproduced results.

How to verify:
1. Open README.md and read through it -- does it make sense as a standalone reproduction guide?
2. Check that the Quick Start section prominently features `federated-ids-run-all`
3. Verify the Troubleshooting section covers the 4 required topics (missing CSVs, CUDA OOM, TensorBoard ports, Python version)
4. Confirm screenshot embeds reference docs/ folder images
5. If screenshots are placeholders, run the pipeline once with real data and copy plots to docs/: `mkdir -p docs && cp outputs/plots/confusion_matrix.png docs/ && cp outputs/plots/convergence.png docs/`

Resume signal: Type "approved" to complete Phase 5, or describe any changes needed
  </action>
  <verify>User confirms README is approved</verify>
  <done>User has reviewed and approved the README as a complete thesis-reproducibility guide</done>
</task>

</tasks>

<verification>
1. README.md exists with 150+ lines
2. README contains all required sections (setup, quick start, configuration, troubleshooting)
3. docs/ directory exists
4. Screenshot references in README point to docs/ folder
5. User confirms README quality and completeness
</verification>

<success_criteria>
- README serves as a complete thesis-reproducibility guide
- Someone unfamiliar with the project can follow the README to reproduce the experiment
- All user-specified troubleshooting topics are covered
- Output screenshots are embedded (real or with instructions to generate)
- User has approved the documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-polish/05-02-SUMMARY.md`
</output>
