---
phase: 05-integration-and-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_example_plots.py
  - docs/confusion_matrix.png
  - docs/convergence.png
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MODL-03, INFR-02]
gap_closure: true

must_haves:
  truths:
    - "README screenshot embeds resolve to actual PNG files in docs/"
    - "REQUIREMENTS.md traceability table reflects Phase 5 validation of MODL-03 and INFR-02"
  artifacts:
    - path: "docs/confusion_matrix.png"
      provides: "Confusion matrix screenshot for README embedding"
    - path: "docs/convergence.png"
      provides: "Convergence plot screenshot for README embedding"
    - path: "scripts/generate_example_plots.py"
      provides: "Reproducible script to regenerate example plots"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with Phase 5 cross-references"
  key_links:
    - from: "README.md"
      to: "docs/confusion_matrix.png"
      via: "Markdown image embed"
      pattern: "docs/confusion_matrix\\.png"
    - from: "README.md"
      to: "docs/convergence.png"
      via: "Markdown image embed"
      pattern: "docs/convergence\\.png"
---

<objective>
Close two verification gaps from 05-VERIFICATION.md:

1. Generate example screenshot PNGs (confusion matrix and convergence plot) in docs/ so the README image embeds resolve to actual files.
2. Update REQUIREMENTS.md traceability table to note that Phase 5 validates/extends MODL-03 and INFR-02.

Purpose: The README currently has broken image references because docs/ is empty. The traceability table is inconsistent with Phase 5 plan frontmatter claiming MODL-03 and INFR-02.
Output: Two PNG files in docs/, a reusable generation script in scripts/, and an updated REQUIREMENTS.md.
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-polish/05-VERIFICATION.md
</context>

<interfaces>
<!-- Key functions from eval/plots.py that Task 1 will call -->

From src/federated_ids/eval/plots.py:
```python
def plot_confusion_matrix(
    y_true: np.ndarray,
    y_pred: np.ndarray,
    output_path: str,
) -> None:
    """Save a confusion matrix heatmap with percentage and count annotations."""

def plot_convergence(
    metrics_path: str,
    output_path: str,
) -> None:
    """Save a two-subplot convergence plot (loss + F1 over FL rounds).
    Reads fl_metrics.json with structure: {"rounds": [{"round": int, "loss": float, "f1": float}, ...]}
    """
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate example screenshot PNGs for README embedding</name>
  <files>scripts/generate_example_plots.py, docs/confusion_matrix.png, docs/convergence.png</files>
  <action>
Create a standalone Python script at `scripts/generate_example_plots.py` that generates realistic-looking example plots using the project's own plotting functions with synthetic data. The script must:

1. Create `scripts/` directory if it does not exist.
2. Import `plot_confusion_matrix` from `federated_ids.eval.plots` and call it with synthetic y_true/y_pred arrays that produce a realistic confusion matrix (e.g., 85% benign correct, 10% DDoS correct, with small misclassification rates -- mimicking a decent FL model). Use roughly 1000 samples total: ~600 BENIGN, ~400 DDoS. Make predictions realistic: ~95% accuracy on benign, ~90% accuracy on DDoS.
3. Import `plot_convergence` from `federated_ids.eval.plots`. It requires a metrics JSON file. Create a temporary JSON file in a tempfile directory with the structure `{"rounds": [{"round": N, "loss": float, "f1": float}, ...]}` for 10 rounds. Use values that show clear convergence: loss decreasing from ~0.6 to ~0.15, F1 increasing from ~0.70 to ~0.93.
4. Output both PNGs directly to `docs/confusion_matrix.png` and `docs/convergence.png`.
5. The script should be runnable standalone: `python scripts/generate_example_plots.py` from project root.
6. Print a message confirming which files were generated.
7. Use `numpy.random.RandomState(42)` for reproducibility.
8. Clean up the temporary metrics JSON file after plot generation.

After writing the script, run it to generate the actual PNGs:
```bash
cd project_root && .venv/Scripts/python scripts/generate_example_plots.py
```

Verify both PNGs exist and are non-empty.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "import os; assert os.path.isfile('docs/confusion_matrix.png') and os.path.getsize('docs/confusion_matrix.png') > 0, 'confusion_matrix.png missing or empty'; assert os.path.isfile('docs/convergence.png') and os.path.getsize('docs/convergence.png') > 0, 'convergence.png missing or empty'; print('Both PNGs exist and are non-empty')"</automated>
  </verify>
  <done>docs/confusion_matrix.png and docs/convergence.png exist as valid, non-empty PNG files. README image embeds now resolve to actual files. scripts/generate_example_plots.py can regenerate them at any time.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table for Phase 5 cross-references</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit the traceability table in `.planning/REQUIREMENTS.md` to note that Phase 5 validates/extends MODL-03 and INFR-02. Specifically:

1. Change the MODL-03 row from:
   `| MODL-03 | Phase 2 | Complete |`
   to:
   `| MODL-03 | Phase 2 (impl), Phase 5 (validated) | Complete |`

2. Change the INFR-02 row from:
   `| INFR-02 | Phase 1 | Complete |`
   to:
   `| INFR-02 | Phase 1 (impl), Phase 5 (validated) | Complete |`

3. Update the `*Last updated:*` line at the bottom to reflect this update.

Do NOT change any other rows or sections. This is a documentation-only fix.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "content = open('.planning/REQUIREMENTS.md').read(); assert 'Phase 5 (validated)' in content, 'Phase 5 validation note missing'; assert content.count('Phase 5 (validated)') == 2, 'Expected exactly 2 Phase 5 annotations'; print('Traceability table updated correctly')"</automated>
  </verify>
  <done>REQUIREMENTS.md traceability table shows MODL-03 and INFR-02 as implemented in their original phases and validated in Phase 5. The inconsistency between plan frontmatter and the traceability table is resolved.</done>
</task>

</tasks>

<verification>
1. `docs/confusion_matrix.png` exists and is a valid non-empty PNG
2. `docs/convergence.png` exists and is a valid non-empty PNG
3. README.md image embeds (`![Confusion Matrix](docs/confusion_matrix.png)` and `![Convergence](docs/convergence.png)`) now resolve to actual files
4. REQUIREMENTS.md traceability table mentions "Phase 5 (validated)" for both MODL-03 and INFR-02
5. `scripts/generate_example_plots.py` runs without error and regenerates the PNGs
</verification>

<success_criteria>
- Both docs/ PNGs exist and are non-empty (verification gap 1 closed)
- REQUIREMENTS.md traceability is consistent with Phase 5 plan frontmatter (verification gap 2 closed)
- scripts/generate_example_plots.py is a reusable artifact for regeneration
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-polish/05-03-SUMMARY.md`
</output>
